# Test and lint tooling. Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
# httpx (used by the test client) is already a runtime dependency.
pytest==9.1.1
pytest-asyncio==1.4.0
ruff==0.16.8
