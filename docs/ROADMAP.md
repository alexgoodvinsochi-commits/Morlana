# Roadmap — Morlana

Статус сверен с кодом 22 сентября 2026. Подробный чек-лист по каждому этапу — на странице плана:
https://claude.ai/artifact/LqFp3r5WRWLZ11BCyXCj7M

## Порядок этапов

1. **Этап 0. Безопасность** — готово в коде (`0ad030a`, `a0bde8c`). Личность только из проверенного
   Telegram initData, лимиты на `/auth`, backend не стартует с `DEV_MODE` и публичным origin,
   логгер `httpx` приглушён. В плане не отмечен один пункт вне кода, за владельцем: пополнить баланс
   провайдера LLM и поставить лимит бюджета в его кабинете.
2. **Этап 1. Фундамент** — готово (`71b75e9`, `0bcc378`). Схему меняет только Alembic, миграции
   применены к рабочей базе (ревизия `e3f4a5b6c7d8`), история раскладов больше не удаляется,
   неверный переход состояния отвечает 409, тесты pytest на настоящих PostgreSQL и Redis, CI в
   GitHub Actions.
3. **Этап 2. Контракт домена** — следующий. Единый тип карты
   `DrawnCard {deck_id, card_id, position, reversed, name, image}`, реестр раскладов из
   `prompts/spreads/*.json`, хук `useReadingSession` вместо логики внутри `ReadingScreen.tsx`.
4. **Редизайн экрана расклада** — после этапа 2, чтобы новый экран не зашил одну карту.
   Утверждённое превью: https://claude.ai/artifact/S3s73k4Yh69vrxfHVyX9jk
5. **Этап 3. Квоты и надёжность LLM.** Списание квоты, дневные лимиты и общий бюджет, таймауты и
   ограничение параллельных вызовов, генерация, не привязанная к HTTP-соединению, учёт токенов.
6. **Этап 4. Хостинг.** Сервер с доменом и TLS вместо машины за ngrok, продовый compose, бэкапы,
   мониторинг.
7. **Публичный запуск.**
8. **Этап 5** — по мере роста: несколько воркеров, метрики, запасная модель LLM.

Задачи следующего этапа не делаются «по пути» в текущем.

## Текущий статус

- Telegram Mini App: логин и пароль как второй фактор Telegram-аккаунта, онбординг, дашборд,
  расклады (1 карта, до 6 циклов), синтез, история. Архивные расклады не удаляются; экран истории
  показывает последние 3 без подписки и 50 с подпиской (`HISTORY_LIMIT_FREE`,
  `HISTORY_LIMIT_PREMIUM`)
- Backend: FastAPI + PostgreSQL + Redis, LLM — OpenAI-совместимый провайдер (MiMo или Yandex AI
  Studio), выбирается переменными `LLM_*`
- Frontend: React 18 + TypeScript + Vite

---

## Бэклог

### 1. Админ-панель / Статистика

- [ ] **Счётчик сессий всего по базе** — общее количество раскладов (archived) в `tarot_sessions`
- [ ] **Счётчик сессий по каждому пользователю** — количество archived сессий на пользователя
- [ ] **Какие расклады делал пользователь** — список использованных `spread_name` + количество циклов + дата последнего расклада

**Реализация:**
- Новый эндпоинт `GET /api/v1/admin/stats` (или `GET /api/v1/user/stats` для самого пользователя)
- Агрегация: `COUNT(*)` по `tarot_sessions` с `GROUP BY user_id`
- Связь с `reading_cycles` для детализации по раскладам

### 2. Оплата

- [ ] Telegram Payments API (ЮKassa / Robokassa). `routes/payments.py` есть, но роутер не подключён
- [ ] Paywall экран (заглушка `Paywall.tsx` есть, нигде не показывается)
- [ ] Подписка: free tier (3 расклада) → платная подписка. Квоты сейчас нет: `free_requests_left`
  не проверяется и не уменьшается. Списание квоты и экран оплаты при её исчерпании — этап 3

### 3. Астрология

- [ ] Сбор даты/времени рождения (интерфейс их не спрашивает)
- [ ] Вычисление знака зодиака на бэкенде: `services/zodiac.py` есть, но вызывается только из
  `/api/v1/astrology/bonus`, который интерфейс не вызывает
- [ ] Персонализация промптов LLM (знак зодиака в промпт сейчас не попадает)

### 4. Дизайн — Mystical Fluidity

Большую часть задач реализовал коммит `87d2c9e`:

- [ ] **T0: Reading Screen** — сделано частично. Карта на 75vh, градиент и название поверх карты,
  прокрутка толкования с затуханием по краям есть (`reading.css`). Свайпа карт нет: в
  `ReadingScreen.tsx` нет обработчиков касаний. Точки под картой статичные: три точки, первая
  всегда активна, ни к чему не привязаны
- [x] **T1: Кнопки** — hover/active + glow (`global.css`)
- [x] **T2: Bottom Nav** — glassmorphism + blur (`bottomnav.css`)
- [x] **T3: Индикатор загрузки** — pulse у спиннера на экране загрузки расклада (`reading.css`)
- [x] **T4: Карта** — overlay с градиентом (`reading.css`)
- [x] **T5: Переходы** — fadeSlideIn у толкования и синтеза (`reading.css`)
- [x] **T6: Loading кнопки** — пульс; с `0ad030a` он висит на `button[aria-busy="true"]`, а не на
  `:disabled` (`global.css`)

Детали исходного плана: `.mimocode/plans/1783595460537-calm-planet.md`

Дальше — редизайн экрана расклада по утверждённому превью
(https://claude.ai/artifact/S3s73k4Yh69vrxfHVyX9jk), после этапа 2. В нём вместо прогресс-бара шесть
точек прогресса по вопросам, а вместо въезда карты — переворот с рубашки.

### 5. Прочее

- [ ] Перенести `SYNTHESIS_PROMPT` из `llm.py` в JSON расклада — входит в этап 2 (реестр раскладов)
- [ ] Synthesis button bug на 6-м цикле. На бэкенде путь проверен тестом
  `test_sixth_cycle_completes_the_reading_automatically`: после 6-го цикла расклад сам переходит в
  `ЗАВЕРШЕНО`, `/synthesis` его принимает. В интерфейсе на живом стеке не перепроверено
- [x] Rate limiting: конфликт slowapi и общего IP за nginx исправлен в `0ad030a` — ключ лимита
  берётся из `X-Forwarded-For`, который дописывает nginx (`rate_limiter.py`). Лимиты стоят только
  на `/auth/*`; лимиты на вызовы LLM — этап 3, хранение лимитов в Redis — этап 5

Снято из списка: «тестовый пользователь testuser — удалить перед продом». Такого логина нет ни в
коде, ни в миграциях, ни в рабочей базе; в истории git он встречается только в этом файле
(проверено 22.09.2026).

### 6. После MVP

- [ ] Рассылки (email / Telegram bot)
- [ ] Push-уведомления
- [ ] Haptic feedback (Telegram WebApp SDK); в превью редизайна — при перевороте карты

---

## Архитектура фронтенда

### CSS: текущий статус

Разделён по компонентам (Вариант 2):

```
styles/
├── global.css          — сброс, body, кнопки, спиннер, ошибки
├── onboarding.css      — LoginScreen + Onboarding
├── dashboard.css       — UserDashboard
├── reading.css         — ReadingScreen
├── history.css         — ReadingHistory
├── bottomnav.css       — BottomNav
└── paywall.css         — Paywall
```

### CSS: миграция на CSS Modules / Tailwind (Вариант 3)

**Чекпоинт для миграции:**
- Количество компонентов > 15
- Или количество CSS файлов > 10
- Или появления конфликтов имён классов

**Признаки готовности:**
- Дублирование стилей между компонентами
- Сложность поиска нужного стиля
- Необходимость изоляции стилей (модалки, попапы)

**Реализация:**
- CSS Modules: каждый `.module.css` привязан к компоненту
- Tailwind: утилитарные классы, кастомизация через конфиг
- Или: CSS-in-JS (styled-components, emotion)
