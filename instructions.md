# Morlana: продукт и предметная область

Этот файл раньше был исходным техническим заданием. ТЗ устарело (другой стек, старая схема, удалённые эндпоинты), поэтому теперь здесь справочник по продукту: что делает Morlana, какими словами мы это называем, как выглядит путь пользователя, какие правила действуют и чего пока нет. Он нужен и людям, и ИИ-агентам, чтобы не выводить правила из кода заново.

Файл живой: когда меняется продуктовое решение, он правится в том же коммите, что и код. Если он расходится с кодом, прав код, а расхождение нужно показать владельцу и исправить. Описано состояние на 22.09.2026, после завершения этапа 2 (контракт домена: расклады и колоды стали данными).

Чего здесь нет:

- `CLAUDE.md` — как работать в репозитории: команды, рецепты задач, инварианты, ловушки окружения.
- `README.md` — запуск, переменные окружения, список эндпоинтов, миграции, тесты, CI.
- `docs/DESIGN.md` — оформление экрана расклада. `docs/ROADMAP.md` — порядок этапов с коммитами (тот же, что в разделе 8) и бэклог задач.
- Схемы таблиц, тела запросов и ответов: их источник — код (`backend/models/user.py`, `backend/schemas/tarot.py`, `backend/routes/`) и тесты в `backend/tests`.

## 1. Что такое Morlana

Morlana — Telegram Mini App «ИИ-Таролог». Человек открывает приложение из бота в Telegram, задаёт вопрос, вытягивает карту Таро и получает толкование, которое пишет языковая модель в роли спокойного прагматичного таролога. В одном раскладе можно задать до шести вопросов подряд, после чего получить общий вывод по всем вопросам — синтез. Завершённые расклады попадают в историю.

Интерфейс, толкования и промпты — на русском языке. Приложение работает только внутри Telegram: личность пользователя подтверждается подписью Telegram, в обычном браузере (при `DEV_MODE=false`) сервер отвечает 401.

## 2. Словарь

| Термин | Что это | В коде |
|---|---|---|
| Расклад | Одна сессия гадания: от «Новый расклад» до синтеза или до того, как его забросили | таблица `tarot_sessions`, `session_id` (UUID) |
| Цикл | Один шаг расклада: вопрос -> карты -> толкование. Сколько циклов разрешено, задаёт расклад (у «Одной карты» — 6) | таблица `reading_cycles`, `cycle_number` |
| Карта | Карта на столе: колода, карта, позиция в раскладе, перевёрнута или нет, имя и картинка. Одна форма для API, Redis и базы | `DrawnCard {deck_id, card_id, position, reversed, name, image}`, `reading_cycles.cards` |
| Позиция | Слот расклада, в котором лежит карта: у «Одной карты» одна позиция `main` («Главная карта») | `positions[].key` и `label` в файле расклада |
| Толкование | Ответ LLM на вопрос цикла с учётом выпавшей карты; приходит потоком | `reading_cycles.interpretation` |
| Синтез | Общий вывод LLM по всем циклам расклада; отправляет расклад в историю | `tarot_sessions.synthesis` |
| Тип расклада (spread) | Шаблон: сколько карт, какие позиции, формат ответа, предел циклов, параметры вызова LLM. Один файл — один расклад. Сейчас один: «Одна карта» | `backend/prompts/spreads/<id>.json`, `tarot_sessions.spread_id` |
| Колода | Набор изображений карт и манифест с их именами. Сейчас одна: Rider-Waite | `frontend/public/decks/<id>/` + `backend/decks/<id>.json`, `tarot_sessions.deck_id` |
| Персона | Роль и жёсткие запреты для LLM | `backend/prompts/persona.md` |
| Активный расклад | Расклад, который ещё можно продолжить | `status = 'active'` и живое состояние в Redis |
| Архивный расклад | Завершённый или заброшенный расклад; виден в истории | `status = 'archived'` |
| История | Список архивных раскладов пользователя, экран «Мои расклады» | `GET /api/v1/tarot/reading/history` |
| Тариф | free или premium. Premium — это `subscription_ends_at` в будущем | `users.subscription_ends_at` |
| initData | Подписанная Telegram строка с данными пользователя; единственное удостоверение личности | заголовок `Authorization: Bearer <initData>` |
| Аккаунт | Строка `users`, привязанная к Telegram id; логин и пароль — второй фактор этого же Telegram-аккаунта | `users.telegram_id`, `login`, `password_hash` |

## 3. Путь пользователя

1. **Вход.** Экран входа: логин и пароль (`POST /auth/login`; в этом файле пути `/auth/...` записаны без префикса `/api/v1`). Нет аккаунта — «Зарегистрироваться»: шаг 1 логин и пароль, шаг 2 имя и пол (`POST /auth/register`). Аккаунт создаётся для того Telegram id, с которого открыто приложение.
2. **Главная.** Профиль (`GET /auth/me`) и кнопки «Новый расклад», «Мои расклады», «Выйти». Нижнее меню: «Главная», «Расклад», «История».
3. **Расклад.** «Новый расклад» всегда начинает с нуля. Вкладка «Расклад» в нижнем меню продолжает незавершённый расклад, если он ещё жив, а иначе сама начинает новый.
4. **Цикл.** Пользователь вводит вопрос -> нажимает «Вытянуть карту» -> нажимает «Получить толкование» и читает текст по мере поступления. Затем выбирает: «Следующий цикл» или «Синтезировать расклад».
5. **Синтез.** Общий вывод по всем циклам. Расклад уходит в историю. Дальше: «Новый расклад», «Мои расклады», «На главную».
6. **История.** Список раскладов по датам; в развёрнутом виде — вопросы, карты и синтез.

## 4. Состояния расклада

Состояние хранится строкой; backend и экран расклада сравнивают одни и те же значения, поэтому это часть контракта между ними. До этапа 2 значения были русскими словами («ОЖИДАНИЕ» и так далее) — теперь это ASCII-идентификаторы.

```
WAITING -> QUESTION_ASKED -> CARDS_DRAWN -> INTERPRETATION -> READY
READY -> WAITING      следующий цикл
READY -> COMPLETED    запрос синтеза или последний разрешённый цикл; состояние конечное
```

Эндпоинты ниже — относительно `/api/v1/tarot/reading`. Подписи кнопок приведены по состоянию на сентябрь 2026; редизайн экрана расклада их изменит, таблица правится вместе с ним.

| Состояние | Что видит пользователь | Что можно сделать | Вызов |
|---|---|---|---|
| `WAITING` | поле «Задайте вопрос картам...» | отправить вопрос | `/ask` |
| `QUESTION_ASKED` | кнопка «Вытянуть карту» | вытянуть карты расклада | `/draw` |
| `CARDS_DRAWN` | карты и кнопка «Получить толкование» | запросить толкование | `/interpret` |
| `INTERPRETATION` | текст поступает; при сбое — «Повторить толкование» | дождаться или повторить | `/interpret` |
| `READY` | «Следующий цикл», «Синтезировать расклад» | новый цикл или синтез | `/next`, `/synthesis` |
| `COMPLETED` | «Синтезировать расклад» (если синтеза ещё нет), «Новый расклад», «Мои расклады», «На главную» | получить синтез, начать новый расклад | `/synthesis`, `/start` |

Вызов не в своём состоянии получает ответ 409, состояние при этом не меняется. Исключение — `/interpret`: пока у цикла нет вопроса или карт (`WAITING`, `QUESTION_ASKED`), он отвечает 400, а из `INTERPRETATION` вызывается повторно. Расклад с истёкшим состоянием отвечает 404, как несуществующий. Каждое действие (`/start`, `/ask`, `/draw`, `/next`) в ответ отдаёт состояние расклада целиком, так что отдельный `GET /state` после него не нужен. Вспомогательные вызовы состояние не меняют: `GET /active` (незавершённый расклад), `GET /state` (полное состояние для восстановления экрана), `GET /history`, `GET /api/v1/tarot/spreads` и `/decks` (что можно выбрать в начале расклада).

## 5. Бизнес-правила

### Личность и аккаунт

1. Пользователя определяет только проверенная подпись Telegram. Сервер никогда не берёт личность из `telegram_id` или логина, присланных в теле запроса.
2. Один Telegram-аккаунт — один аккаунт Morlana. Логин уникален. Войти по логину и паролю можно только с того Telegram-аккаунта, которому они принадлежат; неизвестный логин, чужой логин и неверный пароль выглядят одинаково (401, «Неверный логин или пароль»), чтобы нельзя было перебирать логины.
3. Чужой расклад неотличим от несуществующего: в обоих случаях ответ 404.

### Расклад

4. Карты выбирает сервер случайно и равновероятно (криптографический ГСЧ), по одной на каждую позицию расклада. Клиент не может ни выбрать, ни подменить карту и вообще не знает ни имён карт, ни путей к картинкам: и то и другое приходит с сервера. Расклад и колоду клиент называет один раз, в `/start` (списки — `GET /api/v1/tarot/spreads` и `/decks`), дальше они не меняются: выбор записан и в базе, и в живом состоянии. Неизвестный id — 400, как и расклад, которому нужно больше карт, чем есть в колоде.
5. Сколько карт в цикле, задаёт расклад; в одном цикле карты не повторяются. Перевёрнутая карта возможна, только если расклад это разрешает (`allow_reversed`); у «Одной карты» — нет. Циклы независимы, поэтому одна и та же карта может выпасть в разных циклах одного расклада.
6. Предел циклов задаёт расклад (`max_cycles`; у «Одной карты» — 6). После последнего разрешённого толкования расклад сам переходит в `COMPLETED`, «Следующий цикл» исчезает. Синтез при этом автоматически не запускается: его запрашивает пользователь.
7. Каждое толкование строится только на текущем вопросе и картах текущего цикла (с их позициями); предыдущие циклы в него не передаются. Все циклы вместе видит только синтез.
8. У пользователя один рабочий расклад. Начало нового расклада (кнопка «Новый расклад» или вкладка «Расклад», когда продолжать нечего) закрывает предыдущие: расклады, в которых есть хотя бы один цикл, уходят в историю без синтеза; пустые расклады удаляются, если они старше часа и их состояние уже истекло.
9. Незавершённый расклад живёт 1 час с последнего действия пользователя (вопрос, карта, толкование, следующий цикл). После этого продолжить его нельзя (ответ 404); при следующем начале расклада с ним поступают по правилу 8.
10. Сбой толкования не ломает расклад: он остаётся в состоянии `INTERPRETATION`, и толкование можно запросить снова. Цикл засчитывается, только когда толкование получено и сохранено. Если толкование уже сохранено, а сбой случился после этого, повтор не обращается к LLM заново: цикл завершается сохранённым текстом.
11. Синтез сначала сохраняется, потом показывается: ответ LLM собирается целиком, записывается, расклад уходит в архив, и только затем текст отдаётся пользователю. При сбое или пустом ответе LLM расклад не архивируется и остаётся в `COMPLETED`; синтез можно запросить снова.

### История

12. Архивные расклады не удаляются никогда.
13. Показывается ограниченное число последних раскладов: 3 на бесплатном тарифе, 50 на premium (`HISTORY_LIMIT_FREE`, `HISTORY_LIMIT_PREMIUM`). Ограничение действует только при показе: после перехода на premium старые расклады снова видны.
14. В истории видны дата, число циклов, вопросы, карты и синтез. Тексты толкований хранятся в базе, но в истории не показываются.

### Тарифы и оплата

15. Premium определяется одним условием: `subscription_ends_at` в будущем. От тарифа зависят глубина истории и модель толкования (`LLM_PREMIUM_MODEL` или `LLM_FREE_MODEL`). Синтез сейчас всегда идёт на `LLM_FREE_MODEL` (раздел 7).
16. Оплаты нет: подписку ничто не выставляет, экран Paywall и роут платежей существуют в коде, но не подключены.
17. Квоты запросов нет: поле `users.free_requests_left` заполняется значением 3 при регистрации, но нигде не проверяется и не уменьшается. Лимиты на обращения к LLM появятся на этапе 3.

### Голос и содержание ответов

18. Персона и её жёсткие запреты заданы в `backend/prompts/persona.md`: спокойный прагматичный таролог-консультант; никаких фатальных предсказаний, дат и сроков; никаких медицинских, юридических и финансовых советов, в том числе вкладывать ли деньги и соглашаться ли на лечение или операцию; ответ обычным текстом без Markdown; LLM не задаёт пользователю встречных вопросов. Если человек пишет, что не хочет жить или думает причинить себе вред, персона велит не толковать карты, а коротко поддержать и дать телефоны психологической помощи и 112. Это второй слой защиты: первый — проверка на сервере до обращения к LLM (правило 23), а правило промпта модель может не выполнить. Источник истины — сам файл.
19. И формат толкования, и формат синтеза задаёт файл расклада: `system_prompt` и `synthesis_prompt` в `backend/prompts/spreads/<id>.json`. У «Одной карты» это настроение расклада, суть, риск, ресурс, действие — и синтез из «что повторяется, куда всё движется, что делать», не более 400 слов. Там же лежат `max_tokens` и `temperature`, с которыми зовут модель.
20. В системный промпт толкования попадают персона, правила расклада, описания позиций, имя пользователя и карты строками вида «Главная карта: 7 Мечи» (у перевёрнутой добавляется «(перевёрнута)»); вопрос идёт отдельным сообщением пользователя. У синтеза одно системное сообщение: персона, формат синтеза, имя и все циклы расклада (вопрос, карты, толкование). Знак зодиака и данные о рождении в промпт сейчас не передаются.
21. Итоговый текст LLM очищается: убирается Markdown-разметка и все символы, кроме кириллицы, ASCII и базовой пунктуации, так что эмодзи и иероглифов в сохранённом тексте быть не может. Неразрывный дефис, узкие и тонкие пробелы и многоточие перед этим заменяются обычными `-`, пробелом и `...`, чтобы слова не склеивались. Пока идёт поток, пользователь видит сырые фрагменты; по завершении они заменяются очищенным текстом, и сохраняется именно он.
22. Пустой ответ LLM считается сбоем (правила 10 и 11).
23. Кризисные сообщения. Если в вопросе цикла человек пишет, что не хочет жить, думает о самоубийстве или о том, чтобы причинить себе вред, сервер узнаёт это по списку фраз (`backend/services/safety.py`) ещё до обращения к LLM. Вместо толкования человек получает фиксированный тёплый текст с телефонами психологической помощи и 112, LLM не вызывается. Цикл сохраняется и засчитывается как обычный, расклад можно продолжить. Если такой вопрос есть хотя бы в одном цикле, синтезом расклада становится тот же текст, тоже без LLM. Спорные фразы решаются в пользу заботы: «не хочу жить без него» получит этот текст, «не хочу жить с мужем» или «в этом городе» — обычное толкование. Правило персоны (правило 18) остаётся вторым слоем на случай, если фраза не распознана.

## 6. Чего в продукте сейчас нет

Чтобы никто не опирался на несуществующее:

- оплаты, paywall и квот (правила 16-17);
- астро-онбординга: даты, времени и места рождения приложение не спрашивает. Эндпоинт `POST /api/v1/astrology/bonus` сохранён под будущую астрологию и работает, но интерфейс его не вызывает: он записывает профиль, вычисляет знак зодиака и возвращает заготовленное приветствие без обращения к LLM;
- второго расклада и второй колоды: домен их поддерживает (расклад — один JSON-файл, колода — папка картинок плюс манифест), но в репозитории лежит по одному, и экрана выбора в интерфейсе нет — `/start` всегда идёт с умолчаниями;
- админ-панели и статистики;
- рассылок, push-уведомлений, haptic feedback.

Старого чат-режима тоже нет (раздел 9).

## 7. Известные пробелы

Это факты о текущей реализации, а не требования. Каждый пункт — кандидат в задачи, большинство относится к этапу 3.

Доступ и расход

- Сервер не ведёт сессию входа: эндпоинтам расклада достаточно валидного initData и строки в `users`. Шаг `/auth/login` обязателен только на клиенте, признак «вошёл» — это `localStorage`.
- `POST /api/v1/astrology/bonus` для любого валидного initData создаёт или обновляет строку `users` (только свою: личность берётся из initData) без логина и пароля, и эндпоинты расклада такую строку принимают. То есть расклады доступны в обход `/auth/register`; `/auth/me` для такой строки отвечает 404.
- На эндпоинтах расклада нет rate limit, длина вопроса не ограничена, квот нет: любой пользователь с валидным initData может неограниченно расходовать LLM.
- OpenAPI `/docs` открыт на порту backend `:8000` (настройки FastAPI по умолчанию). Через nginx и ngrok он не виден: проксируются только `/api/` и `/health`.

Расклад и LLM

- Синтез всегда идёт на `LLM_FREE_MODEL`: признак подписки в вызов `/synthesis` не передаётся.
- При пустом `LLM_API_KEY` вместо ответа модели отдаётся строка-заглушка, и она сохраняется как толкование или синтез.
- Кризисные сообщения распознаются по списку фраз (правило 23): другими словами написанное сообщение проверка пропустит, и тогда остаётся только правило персоны. Где жить и с кем, проверка отличает от нежелания жить только внутри одного предложения, поэтому без знаков препинания она ошибается в обе стороны: «расстались с ним жить не хочу» не распознаётся (с запятой распознаётся), а «не хочу жить так как мои родители» получит текст с телефонами вместо толкования. Упоминание суицида ловится всегда, даже в названии фильма или книги.
- Клиент игнорирует ключ `error` в SSE-сообщении и узнаёт о сбое толкования, перечитав `/state` и увидев `INTERPRETATION`.
- У расклада есть поле `tier` (`free` или `premium`), и `GET /spreads` его отдаёт, но нигде не проверяется: расклад, помеченный `premium`, сегодня доступен всем. Гейт — этап 3, вместе с квотами и оплатой; до него не выкладывайте платный расклад в расчёте на защиту.
- `tarot_sessions.spread_name` дублирует `spread_id` (в историю отдаётся именно `spread_name`). Убрать колонку — отдельная миграция.

Данные и инфраструктура

- Выкат этапа 2 теряет расклады «в полёте»: состояние живого расклада переехало с пяти ключей Redis на один документ `reading:{sid}`, и ключ старого формата не читается. Расклад, начатый до выката, отвечает 404 «расклад не найден», пользователь начинает заново. Данные при этом не теряются: завершённые циклы лежат в Postgres.
- `tarot_sessions.updated_at` никогда не обновляется.
- `RedisService` глотает ошибки Redis: его недоступность выглядит как 404 `Reading not found`, а сбой подключения при старте — только предупреждение в логе.
- `/interpret` пишет в сессию БД внутри SSE-генератора, хотя FastAPI 0.115 закрывает её до выполнения тела ответа. Исправление — этап 3, до него версию FastAPI не менять.
- `backend/routes/payments.py` и `frontend/src/components/Paywall.tsx` не подключены, и их контракты не совпадают между собой.
- Шрифты Almendra и Babylonica не содержат кириллицы: русский текст рисуется запасным шрифтом.

## 8. Куда движется продукт

Согласованный порядок работ. Он же с коммитами каждого этапа и бэклогом (оплата, астрология, статистика) — в `docs/ROADMAP.md`; меняется порядок — правь оба файла.

1. Этап 0, безопасность — сделан в коде: личность привязана к проверенному initData, приложение отказывается стартовать с `DEV_MODE` на публичном адресе. Вне кода открыт пункт владельца: баланс и лимит бюджета у провайдера LLM (`docs/ROADMAP.md`).
2. Этап 1, фундамент — сделан: Alembic как единственный путь изменения схемы (рабочая база переведена на новые миграции 22.09.2026), удаление легаси-чата, неудаляемая история, ответ 409 на вызов не в своём состоянии, тесты на настоящих PostgreSQL и Redis, CI на GitHub Actions.
3. Этап 2, контракт домена — сделан: карта в API как объект `DrawnCard {deck_id, card_id, position, reversed, name, image}`; реестры раскладов (`prompts/spreads/*.json`) и колод (`backend/decks/*.json`), собираемые из файлов; состояние расклада одним документом в Redis; хук `useReadingSession` на фронтенде. Новая колода — папка картинок и манифест, новый расклад — один файл.
4. Редизайн экрана расклада (см. `docs/DESIGN.md`).
5. Этап 3: квоты и надёжность LLM, включая работу с сессией БД внутри SSE.
6. Этап 4: хостинг. Затем запуск и этап 5.

## 9. Архив: исходное ТЗ (июнь 2026)

Исторический раздел, не инструкция. Описанного ниже чат-режима в коде нет, и восстанавливать его по старому ТЗ нельзя.

Первая версия файла описывала чат-режим: таблица `chat_histories`, проверка доступа с ответом 402 и экраном оплаты, вытягивание нескольких карт одним запросом, потоковое предсказание с учётом последних сообщений диалога, закрытие сессии кнопкой «Новый расклад», а также LLM-приветствие по знаку зодиака. Этот режим заменён циклическим раскладом. Эндпоинты `/api/v1/tarot/check-access`, `/api/v1/tarot/draw`, `/api/v1/tarot/predict/stream` и `/api/v1/sessions/close` удалены из кода 2026-09-21 на этапе 1. Таблицу `chat_histories` удаляет миграция `e3f4a5b6c7d8`; из рабочей базы она удалена 2026-09-22.

Разошлись с реальностью и остальные положения ТЗ: фронтенд написан на React (варианты Vue и Next.js не понадобились), драйвер базы psycopg 3 вместо asyncpg, вместо GPT и Claude — OpenAI-совместимый провайдер (MiMo или Yandex AI Studio, выбирается переменными окружения), схема выросла (`reading_cycles`, логин и пароль, пол, поля истории).

Принципы ТЗ, которые по-прежнему действуют, перенесены в `CLAUDE.md`: не доверять присланному клиентом `telegram_id`, выбирать карты только на сервере, держать секреты в `.env`, разделять код на слои, делать хирургические правки. Два требования ТЗ записаны только здесь:

- **Расклады не должны быть привязаны к числу карт.** Выполнено на этапе 2: число карт и позиции задаёт файл расклада, код их не знает. В репозитории по-прежнему лежит один расклад на одну карту — это выбор содержания, а не ограничение кода.
- **Неизвестное время рождения.** Когда появится астрология, при `birth_time = NULL` промпт обязан прямо сообщать модели, что время неизвестно и астрологические дома учитывать нельзя. Сейчас требование неактивно: данные о рождении не собираются.

Полный текст прежнего файла (ТЗ с поздними дополнениями о циклическом раскладе и пометками об удалённых эндпоинтах): `git show 0bcc378:instructions.md`.
